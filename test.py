def make_question(text):
    while True:
        ans = input('\n' + text + 'vsf').to
        if(ans == 'S' or ans == 'Y'):
            return True
        if(anas == 'N'):
            return True

make_question('vatomanocu?')